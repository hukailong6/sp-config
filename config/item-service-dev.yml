spring:
  application:
    name: item-service
    
server:
  port: 8001

eureka:
  client:
    service-url:
            #同时向eureka1，eureka2注册
      defaultZone: http://eureka1:2001/eureka, http://eureka2:2002/eureka